@startuml
skinparam defaultFontName "Noto Sans CJK SC"
skinparam defaultFontSize 14
title 前端用例 — 用户操作
skinparam useCaseShape rectangle

actor "未授权用户" as G
actor "用户" as U
actor "维修人员" as M
actor "管理员" as A
actor "Mock充电机" as MC

note top of G : Flutter 客户端实现
note top of U : Flutter 客户端实现
note top of M : Flutter 客户端实现
note top of A : Flutter 客户端实现
note top of MC : Swing 桌面客户端实现\n模拟物理充电机

usecase "注册/登录" as LOG
usecase "启动/结束充电" as CS
usecase "余额校验" as BC
usecase "充值支付" as PAY
usecase "查看充电记录" as VCR
usecase "查看支付记录" as VPR
usecase "提交报修" as SRT
usecase "查看报修记录" as VR
usecase "处理报修" as HR
usecase "充电记录快捷视图" as QV
usecase "充电桩状态快捷查询" as QC
usecase "模拟插枪/拔枪" as MOC
usecase "查询充电状态" as QCS

G --> LOG
U --> CS
U --> PAY
U --> VCR
U --> VPR
U --> SRT
U --> VR
U --> QV
U --> QC
M --> CS
M --> PAY
M --> VCR
M --> VPR
M --> SRT
M --> VR
M --> HR
note right of M : 维修人员仅操作自己的充电/支付记录\n及被分配的报修单，不可越权访问其他用户数据
CS <.left. BC : <<include>>
note bottom of CS : 前置操作：选择充电桩、查看站点列表\n启动前校验余额 >= 10元
M --> QV
M --> QC
A --> HR
A --> QV
A --> QC
MC --> MOC
MC --> CS
note right of MC : Mock充电机使用测试用户余额校验
MC --> QCS
note right of MOC : 模拟物理充电机操作\n插枪后调用启动充电 API\n拔枪后调用结束充电 API\n\n权限边界：\n- JWT scope = mock_charger_only\n- 仅限 /api/v1/charges/* 端点\n- 不参与充值/报修/管理\n- 使用隔离测试用户数据
@enduml