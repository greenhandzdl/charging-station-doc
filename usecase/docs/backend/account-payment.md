# 账户与支付用例

用例：用户注册/登录、账户充值、支付回调

参与者：用户、支付网关、系统

关键点：密码使用散列存储；不在仓库中保存敏感支付信息。

主场景（注册/登录）：

1. 用户注册：填写手机号、密码等信息，系统校验唯一性并保存散列密码。
2. 用户登录：系统验证凭证并返回会话/令牌（可简化为会话 ID）。

主场景（充值）：

1. 用户发起充值请求，选择支付方式。
2. 系统创建充值记录并请求支付网关处理。
3. 支付网关回调确认后，系统更新用户余额并记录支付流水。

异常与安全：

- 支付回调需签名验证，防止伪造回调。
- 所有金额相关变更均写入审计日志。
