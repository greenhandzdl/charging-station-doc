@startuml
skinparam defaultFontName "Noto Sans CJK SC"
skinparam defaultFontSize 14
skinparam linetype ortho

actor "用户/管理员" as UA

frame "前端层" {
  component "Flutter 桌面客户端 (Dart)" as FE
  component "Mock充电机客户端 (Swing)" as MC
  note right of MC
    Swing 桌面客户端
    模拟物理充电机交互
    包含：插枪/拔枪/刷卡界面
    充电进度显示面板
    模拟电量生成逻辑
    调用后端充电 API

    [安全约束]
    - Token scope = mock_charger_only
    - 仅允许 /api/v1/charges/* 端点
    - 使用隔离测试用户数据
    - 不参与充值/报修/管理
    - Mock 客户端应与生产网络隔离
    - 生产环境建议限制来源 IP
  end note
}

frame "接入层" {
  component "Nginx 反向代理" as NGX
  note right of NGX
    负责 SSL 终端、路由分发、
    限流、静态资源服务
  end note
}

frame "后端层" {
  component "Spring Boot" as BE
}

frame "基础设施层" {
  database "PostgreSQL" as DB
  component "Redis" as CACHE
  component "Mock 支付网关" as MOCK
}

UA --> FE
FE -right-> NGX : HTTPS

MC -right-> NGX : HTTPS
note on link #f00: Mock充电机客户端\nNginx 路由规则:\n  location /api/v1/charges (prefix) → 转发至后端 (允许)\n  location /api/v1/ (catch-all) → return 403 (拒绝)\nJWT scope = mock_charger_only 做二次校验\n双层安全隔离：网络层 + 认证层

NGX -right-> BE : 反向代理
note on link #f00: 支付回调接口(/api/v1/payments/callback)仅限内网访问
BE -down-> DB : JDBC/JPA
BE -right-> MOCK : HTTP 回调
BE -down-> CACHE : 会话/缓存

note right of MOCK
  **支付模拟器说明**
  - 当前阶段: 始终返回 success
  - 后期扩展: failed / timeout
  - 回调带模拟签名
  - 后端需设计幂等处理
end note
@enduml