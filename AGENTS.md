# 仓库 AI 助手说明（AGENTS）

目的：为进入此代码仓库的 AI 编程助手提供简明、可执行的工作指引，汇总项目中关于用例文档、PlantUML 渲染与交付要求的关键约束。

核心要点摘要（来自用户要求）

- 目标产物：`usecase` 目录下的用例文档与 UML 图（PlantUML 源放在 `usecase/docs/src/`，渲染图片放在 `usecase/docs/img/`）。
- 渲染脚本：`usecase/scripts/render_plantuml.py`（优先使用系统 `plantuml` 命令；可回退到 `plantuml.jar` 或 Docker）。
- 文档要求：用例文本全部为中文；删除任何“AI 痕迹”或助理措辞；交付文档需为面向评审/交付的正式说明。
- MVP 原则：严格精简实现范围（用户、管理员、支付网关、系统）；只实现端到端可演示的核心用例（注册/登录、充值、启动/结束充电并扣费、查看记录、管理桩、提交报修、生成报表）。
- 审核流程：在进入下一步设计（例如类图、详细类属性）前必须暂停并让用户审核。
- 交付细节：保持文件目录整洁，移除不相关或冗余文档；在 Markdown 中引用已渲染的 SVG 图像而不是嵌入源码；提供渲染和重现步骤。

运行与复现（简要）

- 渲染命令（在仓库根或 `usecase` 目录均可）：

```bash
python3 usecase/scripts/render_plantuml.py
```

提示词模板（供 AI 助手直接复用，中文）

---
你是仓库的 AI 编程助手，按下列规则工作：

1. 目标产出：维护 `usecase` 目录下的用例文档与 UML 图，所有文档中文书写、面向交付。不要在交付文档中出现“AI、我可以、我会”等助理措辞。
2. 文件位置与渲染：PlantUML 源位于 `usecase/docs/src/`，渲染后的图片应放在 `usecase/docs/img/`，渲染脚本为 `usecase/scripts/render_plantuml.py`。修改 `.puml` 后，请运行脚本生成 SVG 并更新 Markdown 中的图片引用。
3. MVP 范围：仅实现核心参与者（用户、管理员、支付网关、系统）与核心用例（注册/登录、充值、启动/结束充电并扣费、查看记录、桩管理、报修、统计）。超出范围的用例请记录为“后续迭代”，不要直接实现或在交付说明中包含实现细节。
4. 审核点：在开始绘制类图或实现非平凡逻辑前（例如数据库设计、类的主要属性），先生成类名与属性草案并停下，明确要求用户审核并确认后再继续。
5. 安全与可扩展性：在实现说明中包含最小安全需求（认证/授权、密码哈希、输入校验、支付回调验证、审计日志）以及可扩展接口建议（DAO/服务层抽象）。
6. 清理与交付：删除或归档不再使用的文档，确保交付包中不包含调试、占位或 AI 讨论性内容；向用户展示清单供最终确认。
7. 交付格式：Markdown 文档、PlantUML 源、生成的 SVG 图、渲染脚本；必要时提供打包（ZIP）供提交。

在完成任何修改后，列出变更的文件清单（路径与简短说明），并等待用户确认或进一步指示。
---

已添加/修改的关键位置（供检查）

- `usecase/docs/01-usecases.md`：主用例概览（中文、精简）。
- `usecase/docs/src/usecase_overall.puml`：总体用例 PlantUML 源（MVP）。
- `usecase/docs/src/basic_info.puml`：基础信息模块 PlantUML 源。 
- `usecase/scripts/render_plantuml.py`：渲染脚本（Python）。

使用建议：将此文件作为 AI 助手进入仓库时的第一参考页，必要时可把其中的提示词复制到会话开始处以确保行为一致。

本阶段提示词（供立即复用）
-----------------------------------------------------------------
下面为本阶段（用例整理、图表渲染、类图草案准备）推荐的短提示词，均为中文，面向执行，便于在会话中直接粘贴使用：

- "检查仓库的用例目录并列出未渲染的 PlantUML 文件。"
- "将所有 `.puml` 按模块放入对应 `docs/<module>/src/` 并在 `docs/<module>/img/` 中生成 SVG。"
- "在修改任何用例文档前，扫描并移除所有 AI 助手措辞（如‘我可以’/‘我会’/‘如需’）。"
- "生成类图草案：列出核心实体名和每个实体的字段（类型与最小安全要求），然后暂停等待用户审核。"
- "将所有顶层散乱的用例文件按模块（overview/frontend/backend/containerd）分类并更新 `README.md` 引用。"
- "在生成或修改图像后，更新对应模块的 README 中的图片链接，使用相对路径指向 `img/*.svg`。"
- "为关键操作（充值、扣费、远程控制）在文档中添加最小审计项（操作人、时间、请求 ID）。"
- "生成交付清单：列出新增/修改/删除的文件（路径+一句话说明），并等待用户确认。"

使用说明：复制所需条目到会话作为下一步指令即可；在执行会影响文件的操作前，总是先展示将要修改的文件列表并征得用户确认。
