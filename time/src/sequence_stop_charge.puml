@startuml
skinparam defaultFontName "Noto Sans CJK SC"
skinparam defaultFontSize 14
title 结束充电与自动扣费时序图 (含 Mock 充电机客户端)

' 所有 SQL 操作使用 PreparedStatement 参数绑定，禁止字符串拼接
actor 用户 as U
participant "Mock充电机\n(Swing 客户端)" as MC
participant "Flutter 客户端" as FE
participant "充电控制器" as Ctrl
participant "安全过滤器链" as Security
participant "充电服务" as Service
participant "计费服务" as Bill
participant "支付服务" as Pay
database 数据库 as DB

U -> MC: 点击"模拟拔枪"按钮
note right U : 用户通过 Mock 充电机\n拔枪触发结束充电

group Mock充电机交互 [Swing UI]
  MC -> MC: ChargeSimulator.stopSimulation()\n获取模拟电量数据
  note right MC : simulatedEnergy = 15.0 kWh\nsimulatedDuration = 30 min

  MC -> MC: 更新 ChargerUIPanel\n显示"拔枪中..."动画
end
note right of MC : 交互模式不对称说明：\n启动流程中 Mock 客户端直接调用后端 API\n结束流程中 Mock 客户端通过 Flutter 中转\n（用户确认弹窗后再由 Flutter 发送 API）\n原因：结束充电涉及扣费确认，需要用户\n二次确认，与启动流程（插枪即启动）不同

MC -> FE: 通知 Flutter 充电结束\n{recordId, energy: 15.0}
FE -> U: 显示"结束充电确认"弹窗
U -> FE: 确认结束

FE -> Ctrl: HTTP POST /api/v1/charges/stop\n{recordId}
note right FE : ApiService.stopCharge()\n请求自动附加 JWT Token\n必须使用 HTTPS 加密传输，防止凭证泄露

group 授权检查 [Spring Security Filter Chain]
  Ctrl -> Security: JwtAuthenticationFilter 解析 Token\n提取 principal (userId, role)

  Security -> Security: @PreAuthorize("@chargeGuard.canStop(authentication, #req.recordId)")
  note right Security : ChargeGuard bean 在注解层校验:\n- 普通用户仅能结束自己的充电记录\n- 管理员可结束任意充电记录\n- recordId 来自请求体，由 ChargeGuard 查询归属\n不通过则返回 403 Forbidden
end

Ctrl -> Service: 请求结束充电 (已通过授权)

Service -> Pay: 校验并扣费 {userId, amount=22.50}

group 事务 [atomic] 覆盖: 查询记录(FOR UPDATE) + 锁定用户余额 + 更新充电记录 + 扣减余额 + 插入支付记录 + 更新桩状态 + 插入审计日志
  Service -> DB: SELECT cr.*, c.status, u.balance\nFROM charge_records cr\nJOIN chargers c ON cr.charger_id = c.id\nJOIN users u ON cr.user_id = u.id\nWHERE cr.id = ? FOR UPDATE -- 使用 PreparedStatement 参数绑定，带行锁，确保事务内独占访问
  note right DB : FOR UPDATE 行锁锁定 charge_records + chargers + users 三表关联行\n锁释放由事务提交/回滚自动管理\nPostgreSQL MVCC 保证更新仅提交后可见
  DB --> Service: 充电记录、桩状态、用户余额

  Service -> DB: SELECT balance FROM users WHERE id = ? FOR UPDATE -- 显式锁定用户余额行，防止 TOCTOU 竞态
  note right DB : 在 JOIN FOR UPDATE 基础上再次显式锁定用户行\n双重保证并发扣费场景下的原子性
  DB --> Service: 余额确认

  Service -> Bill: 计算充电量与费用
  Bill --> Service: energyKwh=15.0, fee=22.50

  alt 余额充足
    Pay -> DB: UPDATE charge_records SET end_time=now(), energy_kwh=15.0, fee=22.50, status='completed' WHERE id = ? -- 使用 PreparedStatement 参数绑定
    DB --> Pay: 更新成功

    Pay -> DB: UPDATE users SET balance = balance - 22.50 WHERE id = ? AND balance >= 22.50 -- 使用 PreparedStatement 参数绑定，原子扣减
    DB --> Pay: 扣费成功

    Pay -> DB: INSERT INTO payments (id, user_id, charge_record_id, method, amount, status) VALUES (?, ?, ?, 'auto_deduct', 22.50, 'success') -- 使用 PreparedStatement 参数绑定
    DB --> Pay: 记录成功

    Pay --> Service: 扣费成功

    Service -> DB: UPDATE chargers SET status = 'idle' WHERE id = ? AND status = 'charging' -- 使用 PreparedStatement 参数绑定，乐观写条件
    DB --> Service: 桩已释放

    Service -> DB: INSERT INTO audit_logs (actor_id, actor_type, action, resource, resource_id, payload) VALUES (?, 'user', 'stop_charge_deducted', 'charge_record', ?, ?::jsonb) -- 使用 PreparedStatement 参数绑定
  else 余额不足
    Pay --> Service: 余额不足

    Service -> DB: UPDATE charge_records SET end_time=now(), energy_kwh=15.0, fee=22.50, status='completed', deduction_status='arrears' WHERE id = ? -- 使用 PreparedStatement 参数绑定
    note right DB : fee 记录应收金额(22.50元)，deduction_status=arrears 标记欠费\n用户充值后通过自动扣费或后台补扣完成扣款
    DB --> Service: 标记欠费

    Service -> DB: UPDATE users SET frozen_until = now() + INTERVAL '30 days' WHERE id = ? -- 使用 PreparedStatement 参数绑定，欠费用户冻结30天
    note right DB : frozen_until 设置后该用户无法启动新充电\n充值后系统清除 frozen_until 解冻
    DB --> Service: 冻结设置成功

    Service -> DB: UPDATE chargers SET status = 'idle' WHERE id = ? AND status = 'charging' -- 使用 PreparedStatement 参数绑定，欠费时释放桩物理状态
    DB --> Service: 桩已释放

    Service -> DB: INSERT INTO audit_logs (actor_id, actor_type, action, resource, resource_id, payload) VALUES (?, 'user', 'charge_arrears', 'charge_record', ?, ?::jsonb) -- 使用 PreparedStatement 参数绑定
  end
end

alt 余额充足
  Service --> Ctrl: 充电结束 {fee=22.50, energy=15.0}
  Ctrl --> FE: HTTP 200 JSON {fee, energy, deductionStatus: 'paid'}
  FE --> U: 显示"充电完成，已扣费 22.50 元"
  FE -> MC: 推送扣费结果
  MC -> MC: ChargerUIPanel 显示\n"充电完成\n费用: 22.50元\n电量: 15.0度"
else 余额不足
  Service --> Ctrl: 欠费提示
  Ctrl --> FE: HTTP 200 JSON {fee, energy, deductionStatus: 'arrears'}
  FE --> U: 显示"余额不足，已标记欠费，请联系管理员充值"
  FE -> MC: 推送欠费结果
  MC -> MC: ChargerUIPanel 显示\n"欠费: 22.50元\n请充值后再使用"
end
@enduml